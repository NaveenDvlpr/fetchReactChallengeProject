import React from 'react';

const Users = ({setContent}) => {
  return (
    <div
        className='users'
    >
        <button
            
        >
            Users
        </button>
    </div>
  );
}

export default Users;

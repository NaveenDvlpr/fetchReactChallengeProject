import React from 'react';

const Posts = ({setContent}) => {
  return (
    <div>
      <button
        className='posts'
      >
        Posts
      </button>
    </div>
  );
}

export default Posts;
